<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["users/index", "Kembali"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["users/new", "Tambahkan User "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Hasil Pencarian</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Nama</th>
            <th>Email</th>
            <th>Alamat</th>

                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $user): ?>
            <tr>
                <td><?php echo $user->id ?></td>
                <td><?php echo $user->name ?></td>
                <td><?php echo $user->email ?></td>
                <td><?php echo $user->address ?></td>

                <td><?php echo $this->tag->linkTo(["users/edit/" . $user->id, "Edit"]); ?>  |
                    <?php echo $this->tag->linkTo(["users/delete/" . $user->id, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("users/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("users/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("users/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("users/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
